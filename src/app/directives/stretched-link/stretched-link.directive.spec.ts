import { StretchedLinkDirective } from './stretched-link.directive';

describe('StretchedLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new StretchedLinkDirective();
    expect(directive).toBeTruthy();
  });
});
